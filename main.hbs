<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta http-equiv="Refresh" content="10"/>
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=0.8">
        <title>Tic-tac-toe</title>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet">
    </head>
    <body>
        <style>
            header {
                background-color: #4169E1;
            }
            body {
                background-color: #1f326b;
            }
            .gameBox {
                background-color: #4169E1;
                width: 450px;
            }
            span {
                background-color: blueviolet;
            }
        </style>
        <header class="text-white border-bottom">
            <div class="d-flex justify-content-between">
                <button onclick='window.location = "/newgame";' type="button" class="btn btn-outline-light btn-lg m-2 shadow-none">New Game</button>
                <div>
                    <form class="d-flex m-2" method="post">
                        <input list="autosearch" name="search" id="search" class="form-control me-2" type="search" placeholder="Search by tags" aria-label="Search" autocomplete="off">
                        <datalist id="autosearch">
                            {{#each tags}}
                                <option value="{{this}}">
                            {{/each}}
                        </datalist>
                        <button class="btn btn-outline-light btn-lg shadow-none" type="submit">Search</button>
                    </form>
                </div>
            </div>
        </header>
        <div class="d-flex align-content-around flex-wrap ms-2 mt-2">
            {{#each game}}
                <div class="gameBox border text-white d-flex flex-column bd-highlight p-2 m-2 ms-0 mt-0 rounded">
                    <div class='h6'>
                        <div class="m-1">NAME: {{this.game_name}}</div>
                        <div class="m-1">TAGS: {{#each this.game_tags}}<span class="tag rounded-pill m-1 ps-2 pe-2">{{this}}</span>{{/each}}</div>
                    </div>
                    <button onclick='window.location = "/game?id={{this.id}}";' class="btn btn-outline-light shadow-none" type="button">Connect</button>
                </div>
            {{/each}}
        </div>
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js"></script>
    </body>
</html>